version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Tests
          command: yarn test
      - run:
          name: Deploy
          command: date
